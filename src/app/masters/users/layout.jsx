export const metadata = {
  title: 'User Management | AccentCRM',
  description: 'Manage users, roles, and permissions',
};

export default function UserMasterLayout({ children }) {
  return children;
}
